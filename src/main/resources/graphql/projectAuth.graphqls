type Auth {
    id: ID!
    projectId: ID!
    project: Project
    userId: ID!
    user: User
    role: ROLE
}

enum ROLE {
    OWNER
    MEMBER
}

extend type Query {
    getAuthByUserId: [Auth]
    getAuthUserByProjectId(id: ID!): [Auth]
}
